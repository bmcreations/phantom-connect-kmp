kotlin.mpp.androidSourceSetLayoutVersion=2
android.useAndroidX=true
kotlin.mpp.applyDefaultHierarchyTemplate=true
org.gradle.jvmargs=-Xmx2g

# Versioning: KSP-style {upstream}-{revision}
# upstream.version tracks the phantom-connect-sdk JS release we're based on
# sdk.revision tracks our KMP-specific releases (reset to 1.0.0 on upstream bump)
upstream.version=2.0.2
sdk.revision=1.0.1
